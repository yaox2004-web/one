name: 持仓股四维报告

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: data-push
  cancel-in-progress: false

jobs:
  report:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy requests    # <--- 加上了 requests
      
      - name: Run report and AI comment
        run: |
          cd scripts
          python3 level4d_report_html.py
          python3 ai_comment.py                # <--- 加上了这行，关键！
      
      - name: 提交 + 推送
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/analysis/
          if git diff --cached --quiet; then
            echo "无变化"
          else
            git commit -m "report: 四维循环看盘报告+AI研判 $(date -u +%Y%m%d)"
            git pull --rebase origin main
            git push
          fi
